<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title><%= pageTitle %> - NetTec Admin</title>
<script src="https://cdn.tailwindcss.com?plugins=forms"></script>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet"/>
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet"/>
<style>
  :root {
    --navy: #0b1f3a;
    --sun: #ffc107;
    --mint: #c7f9cc;
    --rose: #ffd6e0;
    --sky: #dbeafe;
  }
  body {
    font-family: 'Poppins', sans-serif;
    background:
      radial-gradient(520px circle at 12% 8%, rgba(255, 193, 7, 0.22), transparent 62%),
      radial-gradient(460px circle at 88% 16%, rgba(11, 31, 58, 0.14), transparent 62%),
      linear-gradient(180deg, #f8fafc 0%, #eef2ff 100%);
  }
  .sidebar-item-active {
    background-color: rgba(255, 255, 255, 0.1);
    border-left: 4px solid var(--sun);
  }
  .status-pill {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    border-radius: 999px;
    padding: 6px 12px;
    font-weight: 700;
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }
</style>
/*
</head>+
<body class="antialiased text-slate-800">
<div class="flex min-h-screen">
  <%- include('../partials/sidebar') %>

  <main class="flex-1 ml-64 flex flex-col">
    <%- include('../partials/header') %>

    <section class="p-8">
      <div class="max-w-5xl">
        <div class="flex items-center justify-between mb-6">
          <a href="/admin/orders" class="inline-flex items-center gap-2 px-4 py-2 rounded-xl bg-white/90 border border-slate-200 text-slate-700 hover:text-slate-900 hover:shadow-sm transition">
            <span class="material-symbols-outlined text-base">arrow_back</span>
            Back to Orders
          </a>
          <div class="text-xs uppercase tracking-wider font-bold text-slate-500">Order Center</div>
        </div>

        <div class="rounded-3xl overflow-hidden shadow-xl border border-slate-200 bg-white/95 backdrop-blur-sm">
          <div class="bg-gradient-to-r from-[#0b1f3a] via-[#1e3a8a] to-[#0ea5e9] text-white p-7">
            <div class="flex items-center justify-between">
              <div>
                <p class="text-xs uppercase tracking-[0.25em] text-slate-200">Order Details</p>
                <h1 class="text-3xl font-extrabold mt-1">Order #<%= order.id %></h1>
              </div>
              <div class="text-right">
                <p class="text-xs text-slate-200">Created</p>
                <p class="font-semibold"><%= new Date(order.created_at).toLocaleString() %></p>
              </div>
            </div>
          </div>

          <div class="p-7 grid grid-cols-1 lg:grid-cols-3 gap-5">
            <div class="lg:col-span-2 grid grid-cols-1 md:grid-cols-2 gap-4">
              <div class="rounded-2xl border border-slate-200 bg-sky-50 p-4">
                <p class="text-xs uppercase tracking-wide text-slate-500 font-semibold">Customer</p>
                <p class="text-lg font-bold text-slate-900 mt-1"><%= order.customerName || 'N/A' %></p>
              </div>

              <div class="rounded-2xl border border-slate-200 bg-emerald-50 p-4">
                <p class="text-xs uppercase tracking-wide text-slate-500 font-semibold">Total</p>
                <p class="text-lg font-extrabold text-emerald-700 mt-1">LKR <%= Number(order.total).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></p>
              </div>

              <div class="rounded-2xl border border-slate-200 bg-rose-50 p-4">
                <p class="text-xs uppercase tracking-wide text-slate-500 font-semibold">Email</p>
                <p class="text-base font-semibold text-slate-800 mt-1 break-all"><%= order.email || 'N/A' %></p>
              </div>

              <div class="rounded-2xl border border-slate-200 bg-violet-50 p-4">
                <p class="text-xs uppercase tracking-wide text-slate-500 font-semibold">Phone</p>
                <p class="text-base font-semibold text-slate-800 mt-1"><%= order.phone || 'N/A' %></p>
              </div>
            </div>

            <div class="rounded-2xl border border-slate-200 bg-gradient-to-br from-white to-slate-50 p-5">
              <p class="text-xs uppercase tracking-wide text-slate-500 font-semibold mb-2">Order Status</p>
              <% const st = String(order.status || '').toLowerCase(); %>
              <% if (st === 'completed') { %>
                <span class="status-pill bg-emerald-100 text-emerald-700">
                  <span class="material-symbols-outlined text-sm">task_alt</span>
                  Completed
                </span>
              <% } else if (st === 'cancelled') { %>
                <span class="status-pill bg-red-100 text-red-700">
                  <span class="material-symbols-outlined text-sm">cancel</span>
                  Cancelled
                </span>
              <% } else { %>
                <span class="status-pill bg-amber-100 text-amber-700">
                  <span class="material-symbols-outlined text-sm">schedule</span>
                  Pending
                </span>
              <% } %>

              <div class="mt-5 space-y-2 text-sm">
                <div class="flex justify-between">
                  <span class="text-slate-500">Order ID</span>
                  <span class="font-semibold text-slate-900">#<%= order.id %></span>
                </div>
                <div class="flex justify-between">
                  <span class="text-slate-500">Customer ID</span>
                  <span class="font-semibold text-slate-900"><%= order.customer_id || 'N/A' %></span>
                </div>
              </div>

              <a href="/admin/orders/edit/<%= order.id %>" class="mt-6 inline-flex w-full items-center justify-center gap-2 px-4 py-2.5 rounded-xl bg-yellow-400 text-[#0b1f3a] font-bold hover:bg-yellow-300 transition">
                <span class="material-symbols-outlined text-base">edit</span>
                Edit Order
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
</div>
</body>
</html>
